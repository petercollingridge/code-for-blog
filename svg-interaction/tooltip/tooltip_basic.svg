<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 300 200" onload="init(evt)">
    <script type="text/ecmascript"><![CDATA[

	function init(evt) {
	    if (window.svgDocument == null) {
		    svgDocument = evt.target.ownerDocument;
	    }
	    tooltip = svgDocument.getElementById('tooltip');
	}

	function ShowTooltip(evt) {
	    tooltip.setAttributeNS(null, "x", evt.pageX + 10);
	    tooltip.setAttributeNS(null, "y", evt.pageY + 30);
	    tooltip.setAttributeNS(null, "visibility", "visible");
	}

	function HideTooltip(evt) {
	    tooltip.setAttributeNS(null, "visibility", "hidden");
	}

    ]]></script>

    <rect x="40" y="50" width="80" height="100" fill="#007bbf" onmousemove="ShowTooltip(evt)" onmouseout="HideTooltip()"/>
    <rect x="180" y="50" width="80" height="100" fill="#ec008c" onmousemove="ShowTooltip(evt)" onmouseout="HideTooltip()"/>
    <text class="tooltip" id="tooltip" x="0" y="0" visibility="hidden">
        Tooltip
    </text>
</svg>